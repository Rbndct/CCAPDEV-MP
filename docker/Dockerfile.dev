# Development Dockerfile for Court Reservation System
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files first (for better caching)
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start development server with host flag (allows external access)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
